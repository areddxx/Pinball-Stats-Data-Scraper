# Pinball Stats Data Scraper
Scrapes data from pinballstats.com and saves the data to json files.

## Usage
Run `npm install`, replace url variables in `index.js`, run `npm run fetch-data` to generate json files.